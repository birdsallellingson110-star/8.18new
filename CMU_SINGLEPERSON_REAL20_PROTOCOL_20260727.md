# CMU Panoptic 单人真实训练数据（20相机）

## 序列协议

训练序列：

- `171026_pose1`
- `171026_pose2`
- `171026_pose3`
- `171204_pose1`
- `171204_pose2`
- `171204_pose3`
- `171204_pose4`

测试序列：

- `171204_pose5`
- `171204_pose6`

该训练/测试划分来自项目中的 CMU 单人预处理协议。RUMPL 论文的 CMU
测试遵循单人序列协议，并使用 HD 相机 `3, 6, 12, 13, 23`。

## 下载的20个HD相机

`1, 2, 3, 4, 5, 6, 7, 10, 12, 13, 16, 17, 18, 19, 22, 23, 24, 25, 27, 28`

其中 `3, 6, 12, 13, 23` 是权威论文常用的标准五相机评测集合。其余
15个相机通过以下方式选择：

1. 读取 `calibration_171204_pose5.json` 中31个HD相机中心；
2. 相对相机中心均值进行归一化；
3. 以标准五相机为种子进行最远点采样；
4. 补足到20个空间覆盖较均匀的相机。

注意：AMASS训练配置中的“20视角”是每样本20个随机虚拟相机，不是CMU的
固定20个相机节点。

## 数据位置

- 数据根目录：`/mnt/data/cjydata/cmu_singleperson_real20`
- 下载日志：`/mnt/data/cjydata/cmu_singleperson_real20/download_20260727.log`
- 下载脚本：`/home/lixiaob/cjy/download_cmu_singleperson_real20_20260727.sh`
- tmux会话：`cjy_cmu_real20_download_20260727`

`pose5/pose6` 已有的标准五相机视频通过软链接复用
`/mnt/data/cjydata/cmu_singleperson`，不重复占用硬盘。

## 全帧下载加速策略

- 保留每个视频的完整约30fps连续帧，不抽帧、不转码。
- 官方CMU源在本机实测高并发会触发更严重的限速：
  单文件四连接约 `201 KiB/s`，两文件各四连接仅约 `61 KiB/s`，
  因此使用单文件四连接顺序下载。
- 官方Panoptic工具箱自2024年支持SNU镜像。脚本会在每个新文件开始前
  自动检查SNU镜像；镜像可用时自动使用，否则回退CMU源。
- 下载优先级：
  1. 七个训练序列的标准五相机完整视频；
  2. 七个训练序列补齐到20相机；
  3. `pose5/pose6` 补齐额外15相机。
- 第一、第二阶段完成后分别生成
  `TRAIN_STANDARD5_COMPLETE`、`TRAIN_REAL20_COMPLETE`，从而不必等待
  测试集额外相机即可开始标准时序训练。

## 后续建议的两套实验

1. 论文同分布协议：训练和测试均使用标准五相机，但训练时随机抽取2至5视角。
2. 相机泛化协议：训练只使用另外15个相机，测试只使用标准五相机
   `3, 6, 12, 13, 23`，并报告V2至V5全部相机组合。

不要把 `pose5/pose6` 加入训练，以免测试序列泄漏。
