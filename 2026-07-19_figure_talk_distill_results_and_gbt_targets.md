# 对着框架图的组会讲稿：蒸馏结果与 GBT 偏置预期

配图：`figures/distill_rumpl_learnable_gbt_framework.png`

> 口径说明：蒸馏表格是已经完成的真实单模型结果；最后的 GBT 表格是当前正在验证的预期和验收目标，不是实测结果。

## 一、开场：先用一句话讲清问题

老师，我们现在研究的是少视角多相机 3D 人体姿态估计。我们发现，少视角的困难不只是相机数量少，更重要的是不同相机组合的几何质量和检测可靠性差异很大。原始 RUMPL 在不同两视角组合上的 MPJPE 可以从 32.30 mm 波动到 67.28 mm。因此，我们先用满视角到少视角蒸馏提升困难组合，再把 Geometry-Biased Transformer 的置信度和几何偏置直接加入 RUMPL 的视角融合注意力，使网络显式判断哪些视角更可靠。

## 二、对着图从左到右讲

### 图中第1栏：Multi-view observations

这一栏表示模型输入。每个相机提供二维关节坐标、检测置信度和相机标定参数。我们不直接把二维坐标当普通 token，而是根据相机内外参，把每个二维关节反投影成三维射线：

`r_vj(t) = o_v + t d_vj`

其中 `o_v` 是相机光心，`d_vj` 是第 `v` 个视角中第 `j` 个关节的射线方向。随后把射线原点、方向和检测置信度编码成 ray token。这样做的意义是，输入不依赖固定相机编号，同一个模型能够接收任意相机组合和任意视角数量。

这里可以强调：RUMPL 的优势是 ray representation 已经带入了相机几何，但原始 attention 仍主要依靠数据自己学习，没有直接规定几何一致的视角应该得到更高权重。

### 图中第2栏：Full-view-to-sparse-view distillation

这一栏是训练阶段，推理时不需要 teacher。

我们使用同一个网络做两次前向。满5视角分支作为 teacher，得到信息更完整、几何更稳定的三维姿态；少视角分支作为 student，只输入抽取出的相机组合。teacher 停止梯度，student 同时学习真实3D标签和 teacher 的软三维输出。

在普通随机蒸馏基础上，我们加入 hard-view mining：每个 batch 比较多个候选少视角组合，优先训练当前 teacher-student 差距最大的组合。因此，训练不会一直被简单相机组合主导，而会主动处理 `[6,12]`、`[6,13]` 和 `[12,13]` 这类困难几何。

此外，下肢更容易受到遮挡和射线几何误差影响，我们最终采用 `leg distillation weight = 0.9`。它不是一个集成模型，而是一次训练得到的单模型，推理时仍然只前向一次。

本轮严格 GBT 移植以这个 `hard-view + legw0.9` 为固定 baseline，直接替换其 VFT attention。图中橙色框表示严格模块消融：confidence-only、geometry-only 和两者联合三组实验保持原蒸馏数据、seed、epoch 与评测协议不变。

### 图中第3栏：Learnable geometry-confidence biased RUMPL

这是当前新增的核心模块。原始 attention 只有内容相似度：

`Softmax(QK^T / sqrt(d))`

我们按照 Geometry-Biased Transformer 的形式改成：

`A_l = Softmax(QK^T / sqrt(d) + eta_l^2 M_conf - gamma_l^2 M_dist)`

这里有三部分。

第一部分 `QK^T` 是原始数据驱动注意力，表示 token 特征之间的语义相关性。

第二部分 `+ eta_l^2 M_conf` 是置信度偏置。二维检测置信度高的视角，在 softmax 之前直接获得更高 logit。它与仅把 confidence 当输入 embedding 不同，因为它明确影响注意力分配。

第三部分 `- gamma_l^2 M_dist` 是几何偏置。`M_dist` 是同一关节在不同相机下两条三维射线的最近距离。射线越接近相交，说明两个二维检测在真实三维空间越一致，惩罚越小；射线距离越大，说明至少一个检测可能受到遮挡、抖动或误检，attention 就会降低。

`eta_l` 和 `gamma_l` 是每一层独立学习的参数，平方后保证有效系数非负。也就是说，我们不是人工指定第2、3、4视角分别使用多大偏置，而是让同一套逐层参数统一服务于任意 `K`。这与原论文的技术路线一致。

从 softmax 的角度看，加法并不“简单”：

`A_ij proportional to exp(q_i k_j / sqrt(d)) * exp(eta_l^2 c_j) * exp(-gamma_l^2 D_ij)`

因此，置信度和几何项是在指数空间中对注意力权重进行乘性重标定，把 attention 与真实射线几何直接绑定。

完成 view fusion 后，每个关节得到一个融合特征；后续 Pose Fusion Transformer 建模17个关节之间的人体结构，再通过回归头输出 metric 3D pose。新增模块集中在 VFT，不改变 RUMPL 的主体和推理接口。

### 图中第4栏：Output and evaluation

推理阶段只保留 student：输入任意少视角，经过 ray tokenizer、biased VFT、PFT 和 regression head，直接输出三维姿态，不需要满视角 teacher、不需要 ensemble，也不需要相机组合专用模型。

评测不只报告平均 MPJPE，还报告：全部相机组合改善数量、最差组合变化、噪声下的退化曲线和 attention map。目标是同一个模型在 V2、V3、V4 的全部25个组合上都相对 RUMPL 下降。

## 三、当前最强蒸馏单模型结果

模型：`hard-view + legw0.9`。指标：CMU pose5/pose6，Absolute KP* MPJPE，单位 mm。

| 视角数 | RUMPL平均 | 蒸馏平均 | 平均下降 | 最差组合变化 | 改善组合数 |
|---|---:|---:|---:|---:|---:|
| V2 | 46.913 | **40.947** | **-5.966** | **-0.75** | **10/10** |
| V3 | 33.931 | **32.831** | **-1.100** | +0.25 | 8/10 |
| V4 | 30.960 | **29.606** | **-1.354** | **-0.90** | **5/5** |

汇报时可以直接说：最明显的提升出现在两视角，因为两视角信息最少、配置差异最大，满视角 teacher 提供的额外几何知识价值也最大。V4 原本已经比较稳定，因此绝对提升较小。当前真正的剩余问题集中在 V3 的两个边界组合，这正是我们加入显式几何和置信度偏置的动机。

### V2全部10个组合：真实结果

| 相机组合 | RUMPL | 蒸馏 | 变化 |
|---|---:|---:|---:|
| [3,6] | 40.37 | **36.19** | **-4.18** |
| [3,12] | 46.95 | **42.94** | **-4.01** |
| [3,13] | 39.79 | **37.18** | **-2.61** |
| [3,23] | 32.30 | **31.55** | **-0.75** |
| [6,12] | 67.28 | **54.59** | **-12.69** |
| [6,13] | 53.52 | **41.08** | **-12.44** |
| [6,23] | 39.39 | **35.84** | **-3.55** |
| [12,13] | 59.41 | **49.02** | **-10.39** |
| [12,23] | 46.04 | **42.18** | **-3.86** |
| [13,23] | 44.08 | **38.90** | **-5.18** |

### V3全部10个组合：真实结果

| 相机组合 | RUMPL | 蒸馏 | 变化 |
|---|---:|---:|---:|
| [3,6,12] | 34.25 | 34.50 | **+0.25** |
| [3,6,13] | 32.30 | **30.39** | **-1.91** |
| [3,6,23] | 28.71 | **28.54** | **-0.17** |
| [3,12,13] | 33.29 | **32.76** | **-0.53** |
| [3,12,23] | 30.93 | 31.11 | **+0.18** |
| [3,13,23] | 29.19 | **28.79** | **-0.40** |
| [6,12,13] | 44.64 | **40.83** | **-3.81** |
| [6,12,23] | 34.40 | **34.06** | **-0.34** |
| [6,13,23] | 35.90 | **33.03** | **-2.87** |
| [12,13,23] | 35.70 | **34.30** | **-1.40** |

### V4全部5个组合：真实结果

| 相机组合 | RUMPL | 蒸馏 | 变化 |
|---|---:|---:|---:|
| [3,6,12,13] | 31.51 | **30.47** | **-1.04** |
| [3,6,12,23] | 31.21 | **29.25** | **-1.96** |
| [3,6,13,23] | 28.32 | **27.42** | **-0.90** |
| [3,12,13,23] | 30.73 | **28.99** | **-1.73** |
| [6,12,13,23] | 33.03 | **31.90** | **-1.14** |

## 四、加偏置后的预期结果：汇报时必须称为目标

下面不是已经得到的实验结果，而是根据当前蒸馏结果、历史几何偏置诊断和剩余失败组合制定的合理预期区间。

| 视角数 | 当前蒸馏平均 | GBT偏置预期平均 | 预期相对RUMPL变化 | 预期配置覆盖 |
|---|---:|---:|---:|---:|
| V2 | 40.947 | **40.5–40.9** | **-6.0至-6.4 mm** | 10/10 |
| V3 | 32.831 | **32.3–32.7** | **-1.2至-1.6 mm** | **10/10** |
| V4 | 29.606 | **29.1–29.5** | **-1.5至-1.9 mm** | 5/5 |

最关键的预期不是追求一个夸张的平均值，而是：

1. V2保持现有10/10全降，不能因为新模块破坏已经取得的两视角优势。
2. 把 V3 的 `[3,6,12] +0.25 mm` 和 `[3,12,23] +0.18 mm` 两个反弹修复，使 V3 达到10/10。
3. V4继续保持5/5，并通过冗余视角之间的显式几何判断获得小幅平均提升。
4. 同一个 checkpoint 完成 V2、V3、V4，不针对不同视角数设置专用模型或手工 scale。

针对两个 V3 失败点，可以汇报更具体的目标：

| 相机组合 | RUMPL | 当前蒸馏 | GBT目标 |
|---|---:|---:|---:|
| [3,6,12] | 34.25 | 34.50 | **低于34.25，预期33.9–34.2** |
| [3,12,23] | 30.93 | 31.11 | **低于30.93，预期30.5–30.9** |

## 五、最后30秒总结

我们现在已经完成三个步骤。第一，严格复现 RUMPL，并发现其少视角性能存在明显的相机配置敏感性。第二，使用 full-view-to-sparse-view hard-view distillation，把 V2 从平均46.913 mm降到40.947 mm，全部10个相机对都改善；V4也实现5/5全降。第三，针对 V3 剩余的两个弱几何组合，我们按照 Geometry-Biased Transformer 的原公式，在 RUMPL 的每层 View Fusion Attention 中加入可学习的 confidence bias 和 ray-distance geometry bias。当前正在做 confidence-only、geometry-only 和联合偏置三组严格消融。最终目标不是为不同视角数分别调模型，而是让同一个单模型在 V2、V3、V4 全部25个组合上稳定下降，并通过 attention map 和噪声实验解释它为什么更鲁棒。

## 六、老师追问时的简短回答

**问：为什么 V2 提升比 V3/V4大？**

因为 V2 缺少视角冗余，full-view teacher 提供的额外几何知识价值最大；V3/V4原始误差更低，可提升空间更小。

**问：偏置只是加减法，创新够不够？**

偏置加在 softmax 前的 logit 上，等价于在指数空间对注意力做乘性重标定；而且几何项来自真实三维射线距离，逐层系数由网络学习。贡献不只是公式本身，而是将它与 universal ray representation、少视角蒸馏、全相机组合鲁棒评测结合，并用 attention 和噪声实验验证机制。

**问：现在偏置结果出来了吗？**

严格统一版本正在训练。当前可以汇报蒸馏真实结果和 GBT 的实验设计、代码验证与预期目标，不能把预期区间称为实测结果。

**问：是不是为 V2/V3/V4 分别训练？**

不是。正式路线使用同一个模型、同一套逐层可学习偏置和同一个 attention 公式处理任意视角数，不使用 V2/V3/V4 专用参数。
