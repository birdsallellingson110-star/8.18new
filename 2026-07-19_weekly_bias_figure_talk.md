# 本周组会讲稿：对着框架图讲蒸馏与几何-置信度偏置

配图：`figures/distill_rumpl_learnable_gbt_framework.png`

建议时长：6至8分钟。

## 1. 开场：本周解决什么问题

老师，这张图是我们目前的完整技术路线。我们的任务是少视角多相机3D人体姿态估计，核心问题不只是视角数量少，而是不同相机组合的可靠性差异很大。

我们首先严格复现了RUMPL。它通过三维射线表示不同相机观测，能够接受任意相机数量。但是实验发现，在同样都是两视角的情况下，不同相机组合的MPJPE从32.30 mm到67.28 mm，波动超过一倍。这说明RUMPL虽然使用了射线，但原始Transformer并没有显式判断某个二维检测是否可信，也没有直接利用不同射线之间的三维一致性。

前一阶段我们通过满视角到少视角蒸馏取得了明显提升。本周的重点是在这个蒸馏baseline上加入Geometry-Biased Transformer的核心思想：把检测置信度和真实三维射线距离直接写入attention，使同一个模型对不同相机组合进行可靠性加权。

## 2. 指图第1栏：输入与RUMPL基础

先看图的第一栏。每个相机输入二维关节、检测置信度和相机标定参数。对于第`v`个相机的第`j`个关节，我们把二维坐标反投影成三维射线：

`r_vj(t) = o_v + t d_vj`

其中`o_v`是相机光心，`d_vj`是射线方向。然后把射线原点、方向和置信度编码成ray token。

这种表示不绑定固定camera ID，因此同一个网络能够处理任意相机组合以及不同视角数量。这部分保留RUMPL原始优势，也是后面计算显式几何偏置的基础。

这里的关键问题是：RUMPL虽然把ray作为输入，但原始attention仍然主要是

`Softmax(QK^T / sqrt(d))`

也就是让网络完全从特征中自己学习视角关系。射线是否几何一致、二维检测是否可靠，并没有直接进入attention score。

## 3. 指图第2栏：前期蒸馏方法与真实结果

再看第二栏。这是训练阶段的full-view-to-sparse-view self-distillation，推理时不需要teacher。

同一个网络首先输入完整5视角，得到信息更充分的三维姿态作为teacher；少视角student只输入选定的相机组合，同时学习真实3D标签和teacher输出。teacher分支停止梯度，因此不会形成两个独立模型。

为了避免训练被简单组合主导，我们加入hard-view mining：每个batch从候选相机对中选择当前teacher-student差距最大的组合进行训练。下肢更容易受到遮挡和弱几何影响，因此最终使用`leg distillation weight = 0.9`。

这得到当前最强的蒸馏单模型`hard-view + legw0.9`。真实结果是：

| 视角数 | RUMPL平均MPJPE | 蒸馏平均MPJPE | 平均变化 | 改善组合数 |
|---|---:|---:|---:|---:|
| V2 | 46.913 | **40.947** | **-5.966 mm** | **10/10** |
| V3 | 33.931 | **32.831** | **-1.100 mm** | **8/10** |
| V4 | 30.960 | **29.606** | **-1.354 mm** | **5/5** |

这里必须准确说明：当前蒸馏baseline不是25/25全降，而是23/25。V3还有两个轻微反弹：

- `[3,6,12]`：34.25变为34.50 mm，反弹`+0.25 mm`；
- `[3,12,23]`：30.93变为31.11 mm，反弹`+0.18 mm`。

这两个组合都包含camera 3和12，说明仅靠teacher蒸馏仍然不能完全处理弱几何或者低质量视角。这个失败不是回避的结果，而是本周引入显式偏置的直接动机。

## 4. 指图第3栏：本周偏置模块的具体实现

本周的核心工作在第三栏，也就是Geometry-confidence View Fusion Transformer。

我们重新核对Geometry-Biased Transformer原论文后，把每一层View Fusion Attention改成：

`A_l = Softmax(QK^T / sqrt(d) + eta_l^2 M_conf - gamma_l^2 M_dist)`

这个公式包含三个部分。

第一部分是原始内容注意力`QK^T`，保留RUMPL已经学到的特征关系。

第二部分是置信度偏置`+ eta_l^2 M_conf`。置信度高的二维检测在softmax之前直接提高attention logit。原始RUMPL只是把confidence编码进token，网络可以使用也可以忽略；现在confidence会明确参与视角权重分配。

第三部分是几何偏置`- gamma_l^2 M_dist`。对于同一个关节，我们计算不同相机射线之间的最近距离。两条射线越接近相交，说明两个二维观测在真实三维空间越一致；距离越大，说明观测可能受误检、遮挡或者抖动影响，因此降低对应attention。

这里的加减并不是简单的线性修正。softmax展开后可以写成：

`A_ij proportional to exp(q_i k_j / sqrt(d)) * exp(eta_l^2 c_j) * exp(-gamma_l^2 D_ij)`

所以它实际上是在指数空间对原attention进行乘性重加权，把学习到的语义相关性、二维检测可靠性和真实三维几何一致性结合起来。

### 本周实现相对初版的关键修正

我们一开始做过固定系数的`+confidence -geometry`版本。它能够改变attention，但存在三个问题：系数靠人工设置、不同视角数使用过K-aware scale、几何距离还做过归一化。这些都不够严格对应原论文。

本周重新实现后做了以下修正：

1. `eta_l`和`gamma_l`改为每层独立可学习参数，12层可以学习不同的置信度和几何作用强度。
2. 系数使用平方`eta_l^2`和`gamma_l^2`，保证有效偏置强度非负，与论文公式一致。
3. 使用原始pairwise ray distance，不再做会改变物理尺度的样本内归一化。
4. 射线距离同时处理相交线、异面线和平行线，避免平行射线分母接近零时产生错误距离。
5. 几何偏置只施加在真实view token之间，不为fusion token构造虚假的射线距离。
6. K=2、K=3、K=4乃至K=5共用同一个公式和同一套参数，不再设置V2/V3/V4专用scale。
7. 保持原始RUMPL checkpoint严格兼容，关闭新模块时能够完全恢复baseline行为。

我们已经做了单元测试，包括平行射线距离、异面射线距离、距离矩阵对称性、置信度和几何偏置方向、参数梯度，以及同一个真实RUMPL模型对K=2/3/4的前向与反向。以上测试均通过。

## 5. 本周偏置实验告诉了我们什么

前期固定几何偏置实验给出了两个重要现象。

第一，固定GBT配合辅助训练的一组模型曾经做到V2 10/10、V3 10/10、V4 5/5，也就是相对原始RUMPL实现25/25全降。这证明显式几何偏置确实有能力修复V3/V4的边界组合。

但是，这组模型在V2、V3、V4的平均精度都没有全面超过`hard-view + legw0.9`，所以不能替换当前最强baseline。它解决了最差配置，但固定强度也限制了平均性能。

第二，在20 px等效二维抖动诊断中，固定几何偏置模型的MPJPE退化约8.05 mm，而旧hard-view模型退化约6.22 mm。attention图还显示，某个被扰动视角的权重没有下降，反而从0.3007升到0.3127。

这说明仅仅把一个人工固定的负距离项加进attention，并不保证模型自动学会正确抑制坏视角。问题不在于几何偏置思想无效，而在于固定强度、训练目标和噪声分布没有对齐。因此，本周才进一步改成逐层可学习偏置，并计划加入训练期噪声增强。

## 6. 指图第4栏：当前优化和评测标准

最后看第四栏。推理时只保留一个student模型。输入任意少视角，依次经过ray tokenizer、带偏置的VFT、Pose Fusion Transformer和3D regression head，直接输出三维姿态。不需要满视角teacher，不需要prediction ensemble，也不为不同相机组合训练专家模型。

当前严格消融分为三组：

- confidence-only：验证二维检测可靠性偏置本身的贡献；
- geometry-only：验证三维射线一致性偏置本身的贡献；
- confidence + geometry：验证两者是否互补。

三组完全继承`hard-view + legw0.9`的数据、蒸馏方式、epoch、seed和评测协议，只改变attention偏置，避免把辅助训练或者不同视角scale混入结论。

当前两组前期K-aware对照正在epoch 17/20，它们完成后会自动评测，随后启动上述统一可学习偏置三组实验。前期K-aware结果只作为诊断，不作为最终论文主结果。

最终验收不是只看平均MPJPE，而是同时看四项：

1. 同一个checkpoint在V2、V3、V4全部25个组合上的结果；
2. 相对RUMPL是否达到25/25全降；
3. 是否保持或超过`hard-view + legw0.9`的平均精度；
4. attention是否确实降低低置信度、高射线距离和受噪声扰动视角的权重。

图中右侧的`V2 10/10、V3 10/10、V4 5/5`是Robustness Target，不是当前已经取得的统一可学习GBT结果。

## 7. 现在的进一步优化

当前优化分两步进行。

第一步先完成严格的confidence-only、geometry-only和联合偏置，判断平均精度与worst configuration之间的权衡。如果联合偏置能做到25/25，同时平均值超过现有蒸馏baseline，就形成论文主模型。

第二步加入训练期二维关节噪声和confidence扰动。噪声不是只用于测试，而是在训练中随机模拟检测抖动、遮挡和低置信度，使模型学会在ray distance增大时主动降低对应attention。最终报告clean、轻噪声和重噪声三条MPJPE曲线，并配合attention heatmap解释机制。

attention可视化重点观察：扰动前后目标相机权重变化、attention与confidence的相关性、attention与ray distance的负相关性，以及attention变化是否与MPJPE改善一致。这样论文不仅报告数值提升，还能证明偏置确实按照设计工作。

## 8. 结束总结

本周的主要进展不是简单增加一个attention项，而是完成了从问题诊断到严格模块实现的闭环。

前期蒸馏已经将V2平均MPJPE降低5.97 mm并实现10/10全降，但总体仍是23/25，V3存在两个弱几何失败组合。固定偏置实验说明显式几何可以帮助实现配置全覆盖，但人工强度会损失平均精度，而且未经过噪声训练时不一定真正抑制扰动视角。

因此，我们现在严格按照原论文改成逐层可学习的置信度和射线距离偏置，用同一个公式处理任意视角数，并通过三组严格消融、全部相机组合、噪声曲线和attention图进行验证。最终目标是一个单模型同时兼顾平均精度、最差组合和噪声鲁棒性。

## 9. 一分钟精简版

老师，这周我们主要解决RUMPL虽然输入了ray和confidence，但attention没有显式利用真实几何可靠性的问题。前期`hard-view + legw0.9`蒸馏在V2平均下降5.97 mm并做到10/10全降，V4为5/5，但V3还有两个组合反弹，所以总体是23/25。

我们按照Geometry-Biased Transformer把VFT改成`QK + confidence bias - ray-distance bias`。confidence高的视角提高attention，跨视角射线距离大的观测降低attention；每层偏置系数独立学习并平方保证非负，同一套参数统一处理K=2/3/4，不再人为设置不同视角scale。射线距离的相交、异面和平行情形以及模型梯度均已测试通过。

前期固定偏置曾实现相对RUMPL的25/25全降，但平均值没有超过最强蒸馏，而且噪声诊断表明固定强度不能保证抑制坏视角。因此现在做confidence-only、geometry-only和联合偏置三组严格实验，下一步加入训练期噪声，并用attention图验证模型是否真正降低低置信度和几何不一致视角的权重。最终目标是一个checkpoint在V2、V3、V4全部组合上全降，同时平均精度超过现有蒸馏baseline。
